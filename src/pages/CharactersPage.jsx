import { useState } from 'react'; import { getCharacters } from '../api/api'; import useApi from '../hooks/useApi'; import Loader from '../components/Loader'; import ErrorMessage from '../components/ErrorMessage'; import EmptyState from '../components/EmptyState'; import CharacterCard from '../components/CharacterCard';
export default function CharactersPage(){const {data,loading,error}=useApi(getCharacters,[]); const [search,setSearch]=useState(''); const [category,setCategory]=useState('Todos'); if(loading)return <Loader/>; if(error)return <ErrorMessage message={error}/>; const filtered=(data||[]).filter(c=>c.nombre.toLowerCase().includes(search.toLowerCase())&&(category==='Todos'||c.categoria===category)); if(!filtered.length)return <EmptyState message='No hay personajes'/>; return <section><h1>Personajes</h1><div className='row'><input placeholder='Buscar personaje' value={search} onChange={e=>setSearch(e.target.value)}/><select value={category} onChange={e=>setCategory(e.target.value)}><option>Todos</option><option>Familia</option><option>Escuela</option><option>Profesores</option></select></div><div className='grid'>{filtered.map(c=><CharacterCard key={c.id} character={c}/>)}</div></section>}
