import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { useAuth } from '../context/AuthContext';
export default function LoginPage(){const [username,setUsername]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState(''); const {login}=useAuth(); const nav=useNavigate(); const submit=(e)=>{e.preventDefault(); const res=login(username,password); if(res.ok) nav('/perfil'); else setError(res.message);}; return <section className='panel'><h1>Login</h1><form onSubmit={submit} className='form'><input value={username} onChange={e=>setUsername(e.target.value)} placeholder='usuario'/><input type='password' value={password} onChange={e=>setPassword(e.target.value)} placeholder='contraseña'/><button className='btn'>Entrar</button>{error&&<p className='error'>{error}</p>}</form></section>}
